import { InfrastructureModule } from './infrastructure-module';

describe('loneFiddlerPackagesInfrastructure', () => {
  it('should work', () => {
    expect(InfrastructureModule).toBeDefined();
  });
});
