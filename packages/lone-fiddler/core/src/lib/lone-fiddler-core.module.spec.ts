import { loneFiddlerCoreModule } from './lone-fiddler-core-module';

describe('loneFiddlerPackagesCore', () => {
  it('should work', () => {
    expect(loneFiddlerCoreModule()).toEqual('lone-fiddler-packages-core');
  });
});
