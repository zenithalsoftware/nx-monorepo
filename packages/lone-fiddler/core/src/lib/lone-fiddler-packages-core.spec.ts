import { loneFiddlerPackagesCore } from './lone-fiddler-packages-core';

describe('loneFiddlerPackagesCore', () => {
  it('should work', () => {
    expect(loneFiddlerPackagesCore()).toEqual('lone-fiddler-packages-core');
  });
});
